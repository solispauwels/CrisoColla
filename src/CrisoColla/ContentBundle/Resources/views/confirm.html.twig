<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
    <h3>{{ "Delete" | trans({}, "content") }}</h3>
</div>
<div class="modal-body">
    <p>{{ "Are you sure you want to delete this content?" | trans({}, "content") }}</p>
    <p>{{ "This action is irreversible" | trans({}, "content") }}.</p>
</div>
<div class="modal-footer">
    <a class="btn" data-dismiss="modal">{{ "Cancel" | trans({}, "content") }}</a>
    <a href="#" class="btn delete btn-primary" data-dismiss="modal">{{ "Delete" | trans({}, "content") }}</a>
</div>
